import { LocalePrefix } from "next-intl/routing";

const localePrefix: LocalePrefix = 'as-needed';

export const Config = {
    name: 'Ivan Vulović ',
    locales: ['en', 'fr'],
    defaultLocale: 'en',
    localePrefix,
};